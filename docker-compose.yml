version: '3.8'

services:
  flask:
    image: flask:cadstro_ de_chave_02
    build: .
    volumes:
      - ./flask:/flask
    environment:
       TZ: America/Fortaleza
    restart: always
    ports:
      - "4005:80"
